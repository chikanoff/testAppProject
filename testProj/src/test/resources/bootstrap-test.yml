embedded:
    mysql:
        enabled: true
        docker-image: mysql:latest
        user: mysql
        password: root
    rabbitmq:
        enabled: true
        docker-image: rabbitmq:latest
        user: guest
        password: guest